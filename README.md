Steps

npm install
truffle compile
truffle migrate --reset --network testnet
npm start which should popup http://localhost:3000/ page
make sure to connect metamask to harmony testnet, as the social-network contracts are deployed on harmony testnet